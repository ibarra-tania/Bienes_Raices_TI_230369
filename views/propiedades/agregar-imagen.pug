extends ../layout/admin

block styles
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/dropzone/5.9.3/dropzone.css")
    meta(name="csrf-token" content= csrfToken)
block contenido
    .py-10
        h1.text-4xl.my-10.font-foco.text-center.text-darkblue Bienes 
            span.font-normal.text-black Raíces  
        h2.text-center.text-2xl.font-foco= pagina

        div(class="bg-white shadow py-8 px-4 rounded mx-auto max-w-4xl my-10 md:px-10")
            form(id="imagen" class="dropzone border-dashed border-2 w-full min:h-96 h-auto rounded flex-col justify-center items-center" method="POST" action=`/propiedades/agregar-imagen/${propiedad.id}` data-context="propiedad")
                input(type="hidden" name="_csrf" value= csrfToken)
                

            button(id="publicar" class="w-full transition ease-in-out delay-150 bg-darkblue hover:-translate-y-1 hover:scale-105 hover:bg-babyblue duration-300 text-white font-rubi py-3 rounded-lg cursor-pointer transition duration-300" type="button") Publicar Propiedad   

block scripts 
    script(src="/js/agregarImagen.js")